# DNS server test
DNS-server-test simulates a DNS server using data from the `cache.data` file generated by dns-client-test.
## Usage
```sh
Commands:
  Required parameters:
    -l  "x.x.x.x:xx"  Listen address
```
